/* ============================================================
   Konga Kratom — настройка приёма заявок
   Это единственный файл, который нужно править, чтобы заявки
   падали в Telegram. Подробности: DEPLOY.md
   ============================================================ */
window.KONGA_CONFIG = {

  /* --- ВАРИАНТ A (рекомендуется): свой прокси, токен спрятан ---
     Cloudflare Worker или Google Apps Script — код в папке backend/.
     Вставь сюда URL, который выдаст Cloudflare / Google.
     Пример: 'https://konga-leads.tvoy-akkaunt.workers.dev'            */
  endpoint: '',

  /* --- ВАРИАНТ B: напрямую в Telegram, без прокси ---
     Работает сразу, но ТОКЕН БУДЕТ ВИДЕН всем в коде страницы
     (любой сможет писать от имени бота и читать заявки).
     Годится только для быстрого теста. Используется, если
     endpoint выше пустой.                                            */
  telegram: {
    botToken: '',   // '8123456789:AAH...'  от @BotFather
    chatId: ''      // '123456789' или '-1001234567890' для группы
  },

  /* Текст размера скидки в сообщении (в HTML он захардкожен как 15 %) */
  discount: 15,

  /* Куда звонить, если форма не отправилась — показывается в ошибке */
  fallbackPhone: '+420 000 000 000',

  /* true — печатать подробности отправки в консоль браузера */
  debug: false
};
