/* ============================================================
   Konga Kratom — настройка приёма заявок
   Это единственный файл, который нужно править, чтобы заявки
   падали в Telegram. Подробности: DEPLOY.md
   ============================================================ */
window.KONGA_CONFIG = {

  /* --- Куда шлём заявку. Токен бота здесь НЕ хранится ---
     '/api/lead'  — сайт на Cloudflare Pages: функция functions/api/lead.js
                    на том же домене, токен лежит в секретах Cloudflare.
     полный URL   — сайт на GitHub Pages: адрес отдельного Cloudflare Worker
                    или Google Apps Script, например
                    'https://konga-leads.tvoy-akkaunt.workers.dev'
     Подробности: DEPLOY.md, шаг 2.                                    */
  endpoint: '/api/lead',

  /* --- Запасной путь: напрямую в Telegram, без прослойки ---
     ТОКЕН БУДЕТ ВИДЕН всем в коде страницы: любой сможет писать от
     имени бота, читать заявки и удалять их. Не коммитить.
     Используется, только если endpoint выше пустой.                   */
  telegram: {
    botToken: '',   // '8123456789:AAH...'  от @BotFather
    chatId: ''      // '123456789', несколько — '111,222', или '-1001234567890' для группы
  },

  /* Куда звонить, если форма не отправилась — показывается в ошибке */
  fallbackPhone: '+420 000 000 000',

  /* --- Контакты для всплывающего баннера ---
     (в index.html телефон и адрес прописаны отдельно — меняй в обоих местах) */
  contact: {
    price:     '249 Kč',
    priceUnit: 'za 50 g',
    address:   'Nuselská 31, Praha 4',
    hours:     'Po–Pá 10–19',
    phone:     '+420 000 000 000'
  },

  /* --- Всплывающий баннер при заходе --- */
  popup: {
    enabled: true,
    variant: 'sheet',      // 'sheet' — шторка снизу | 'card' — окно по центру | 'bar' — полоска внизу
    delay: 1200,           // через сколько мс показать

    /* Как часто показывать одному человеку:
         'session' — раз за сеанс браузера (закрыл вкладку, зашёл снова — покажется)
         'always'  — каждый раз, даже при перезагрузке (удобно, пока тестируешь)
          24       — число: столько часов не показывать повторно            */
    showAgain: 'session'
  },

  /* true — печатать подробности отправки в консоль браузера */
  debug: false
};
